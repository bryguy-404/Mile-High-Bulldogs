---
import { siteConfig } from "../lib/site.config";
---

<div class="border-y-4 border-foreground bg-accent text-foreground">
  <div class="grid md:grid-cols-[auto_1fr] md:items-stretch">
    <p
      class="flex items-center justify-center bg-foreground px-5 py-4 font-heading text-4xl font-black uppercase leading-none tracking-tight text-accent sm:px-7 sm:text-5xl md:border-r-8 md:border-primary md:py-6"
    >
      {siteConfig.event.dateBanner}
    </p>
    <p
      class="flex items-center px-5 py-4 font-heading text-3xl font-black uppercase leading-[.9] sm:px-7 sm:text-4xl md:py-6 lg:text-5xl"
    >
      {siteConfig.tagline}
    </p>
  </div>
</div>
